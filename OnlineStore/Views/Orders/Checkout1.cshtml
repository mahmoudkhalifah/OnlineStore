@model OnlineStore.Models.Order

@{
    ViewData["Title"] = "Checkout";
    var addresses = new SelectList(Model.Customer.Addresses, "AddressId", "address");

    Order createOnClick()
    {
        var x = 0;
        x++;
        return Model;
    }
}

<h1>Checkout</h1>

<div>
    <h4>Order</h4>
    <hr />
    <dl class="row">
        
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.PaymentMethod)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.PaymentMethod)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Customer)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Customer.FullName)
        </dd>
        
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Customer.Addresses)
        </dt>
        <dd class = "col-sm-8 form-group">
            @*@Html.DisplayFor(model => model.Customer.Addresses/*[0].ToString()*/)*@

            <select asp-for="@Model.AddressId" class="form-control" asp-items="addresses"
            
                ID="donkey"
            ></select>
            @Html.DropDownListFor(model => model.AddressId , addresses);
        </dd>
        <dd  class = "col-sm-2">
            <a 
                asp-action="Create" asp-controller="Addresses" asp-route-id="@Model?.CustomerId"

            >Add Address</a>
            @*type="button" value="Add Address" class="btn btn-primary" *@

        </dd>
    </dl>
    <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" 
                    onclick="location.href='@Url.Action("Create1", "Orders",Model)'"
                />
     </div>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.OrderId">Edit</a> |
    <a asp-action="Index">Back to List</a>
</div>
