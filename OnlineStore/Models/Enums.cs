namespace OnlineStore.Models
{
    public enum Colors { black,red, blue, green, yellow, cyan, white,pink ,silver,gold}
    public enum OrderState { Processing, Delivered}
    public enum PayMethod { Cash, Visa}

}
